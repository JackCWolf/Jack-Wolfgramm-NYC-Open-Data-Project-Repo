from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predict business success chance based on if other businesses are opening in the area',
    author='Jack Wolfgramm',
    license='',
)
